name: movie_app_core
description: A new Flutter package.
version: 0.0.1

publish_to: "none"

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  json_serializable: ^4.1.3

  #Packages dependencies
  movie_app_dependencies:
    git:
      url: https://github.com/Bwolfs2/movie_app_dependencies.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: any

flutter:
